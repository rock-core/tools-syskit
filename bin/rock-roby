#! /usr/bin/env ruby
APP_DIR = Dir.pwd
mode = ARGV.shift

begin
    require "orocos/roby/scripts/#{mode}"
rescue LoadError
    begin
        require "roby/app/scripts/#{mode}"
    rescue LoadError
        STDERR.puts "unknown mode '#{mode}'"
        exit(1)
    end
end
